package com.fullstack.escape_rentals.booking.entity;

public enum BookingStatus {
    BOOKING_PAYMENT_PENDING,
    PENDING,
    CONFIRMED,
    HOST_CANCELLED,
    COMPLETED,
    GUEST_CANCELLED,
    EXPIRED
}
